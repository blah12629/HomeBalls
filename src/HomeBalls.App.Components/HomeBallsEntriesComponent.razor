@using CEo.Pokemon.HomeBalls.App.Components.Entries
@inherits HomeBallsRootComponent

@{ base.BuildRenderTree(__builder); }

@code
{
    IReadOnlyList<Type> _componentTypes = new[] { typeof(HomeBallsEntries) };

    protected override IReadOnlyList<Type> ComponentTypes => _componentTypes;
}