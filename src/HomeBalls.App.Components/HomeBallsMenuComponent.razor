@using CEo.Pokemon.HomeBalls.App.Components.Menu
@inherits HomeBallsRootComponent

@{ base.BuildRenderTree(__builder); }

@code
{
    IReadOnlyList<Type> _componentTypes = new[]
    {
        typeof(HomeBallsAboutMenu),
        typeof(HomeBallsTradeMenu),
        typeof(HomeBallsEditMenu),
        typeof(HomeBallsSettingsMenu)
    };

    protected override IReadOnlyList<Type> ComponentTypes => _componentTypes;
}

